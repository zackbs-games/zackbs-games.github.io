2026-04-12T18:52:06.506Z TEST: test message
2026-04-12T18:52:12.937Z TEST: test message
2026-04-12T18:52:52.063Z LOG: Loaded shader.wgsl (870 chars)
2026-04-12T18:52:52.067Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-12T18:52:52.069Z LOG: Shader compiled successfully.
2026-04-12T18:53:13.357Z LOG: Loaded shader.wgsl (870 chars)
2026-04-12T18:53:13.359Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-12T18:53:13.361Z LOG: Shader compiled successfully.
2026-04-12T18:54:45.796Z ERROR: Unhandled promise rejection: ReferenceError: mat4Identity is not defined
2026-04-12T18:54:45.797Z ERROR: Unhandled promise rejection: ReferenceError: mat4Identity is not defined
2026-04-12T18:55:15.916Z LOG: Loaded shader.wgsl (870 chars)
2026-04-12T18:55:15.916Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-12T18:55:15.919Z LOG: Shader compiled successfully.
2026-04-12T18:55:42.180Z LOG: Loaded shader.wgsl (870 chars)
2026-04-12T18:55:42.180Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-12T18:55:42.182Z LOG: Shader compiled successfully.
2026-04-12T18:55:59.604Z LOG: Loaded shader.wgsl (870 chars)
2026-04-12T18:55:59.605Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-12T18:55:59.607Z LOG: Shader compiled successfully.
2026-04-12T18:56:28.655Z LOG: Loaded shader.wgsl (870 chars)
2026-04-12T18:56:28.655Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-12T18:56:28.655Z LOG: Shader compiled successfully.
2026-04-12T18:56:28.659Z ERROR: Uncaught TypeError: Failed to execute 'setPipeline' on 'GPURenderPassEncoder': parameter 1 is not of type 'GPURenderPipeline'. @ http://localhost:8000/main.js?cb=2:557:14
2026-04-12T18:56:28.662Z ERROR: Uncaught TypeError: Failed to execute 'setPipeline' on 'GPURenderPassEncoder': parameter 1 is not of type 'GPURenderPipeline'. @ http://localhost:8000/main.js?cb=2:557:14
2026-04-12T19:14:40.766Z LOG: Loaded shader.wgsl (870 chars)
2026-04-12T19:14:40.767Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-12T19:14:40.768Z LOG: Shader compiled successfully.
2026-04-12T19:14:52.472Z LOG: Loaded shader.wgsl (870 chars)
2026-04-12T19:14:52.472Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-12T19:14:52.474Z LOG: Shader compiled successfully.
2026-04-12T19:35:43.728Z LOG: Loaded shader.wgsl (870 chars)
2026-04-12T19:35:43.730Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-12T19:35:43.739Z LOG: Shader compiled successfully.
2026-04-12T19:35:45.089Z LOG: Loaded shader.wgsl (870 chars)
2026-04-12T19:35:45.089Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-12T19:35:45.091Z LOG: Shader compiled successfully.
2026-04-13T07:03:13.632Z LOG: Loaded shader.wgsl (870 chars)
2026-04-13T07:03:13.635Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-13T07:03:13.635Z LOG: Shader compiled successfully.
2026-04-13T07:03:15.945Z LOG: Loaded shader.wgsl (870 chars)
2026-04-13T07:03:15.945Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-13T07:03:15.947Z LOG: Shader compiled successfully.
2026-04-13T07:04:56.168Z LOG: Loaded shader.wgsl (870 chars)
2026-04-13T07:04:56.168Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-13T07:04:56.171Z LOG: Shader compiled successfully.
2026-04-13T07:04:57.565Z LOG: Loaded shader.wgsl (870 chars)
2026-04-13T07:04:57.566Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-13T07:04:57.568Z LOG: Shader compiled successfully.
2026-04-18T05:44:56.062Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T05:44:56.063Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T05:44:56.064Z LOG: Shader compiled successfully.
2026-04-18T05:48:07.421Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T05:48:07.422Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T05:48:07.423Z LOG: Shader compiled successfully.
2026-04-18T05:51:15.970Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T05:51:15.970Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T05:51:15.973Z LOG: Shader compiled successfully.
2026-04-18T05:52:03.376Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T05:52:03.376Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T05:52:03.378Z LOG: Shader compiled successfully.
2026-04-18T05:55:22.674Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T05:55:22.675Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T05:55:22.678Z LOG: Shader compiled successfully.
2026-04-18T05:57:36.369Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T05:57:36.369Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T05:57:36.371Z LOG: Shader compiled successfully.
2026-04-18T05:58:46.016Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T05:58:46.016Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T05:58:46.017Z LOG: Shader compiled successfully.
2026-04-18T06:01:33.949Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T06:01:33.950Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T06:01:33.951Z LOG: Shader compiled successfully.
2026-04-18T06:02:58.046Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T06:02:58.046Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T06:02:58.048Z LOG: Shader compiled successfully.
2026-04-18T06:03:11.245Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T06:03:11.245Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T06:03:11.246Z LOG: Shader compiled successfully.
2026-04-18T06:03:51.060Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T06:03:51.061Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T06:03:51.061Z LOG: Shader compiled successfully.
2026-04-18T06:05:42.651Z LOG: Loaded shader.wgsl (870 chars)
2026-04-18T06:05:42.651Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal : vec3<f32>,
  @location(2) model0 : vec4<f32>,
  @location(3) model1 : vec4<f32>,
  @location(4) model2 : vec4<f32>,
  @location(5) model3 : vec4<f32>,
  @location(6) color : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color : vec4<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  return input.color;
}

2026-04-18T06:05:42.653Z LOG: Shader compiled successfully.
2026-04-18T06:10:35.815Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:10:35.815Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:10:35.817Z LOG: Shader compiled successfully.
2026-04-18T06:14:03.002Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:14:03.003Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:14:03.004Z LOG: Shader compiled successfully.
2026-04-18T06:20:26.177Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:20:26.177Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:20:26.179Z LOG: Shader compiled successfully.
2026-04-18T06:23:36.411Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:23:36.412Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:23:36.413Z LOG: Shader compiled successfully.
2026-04-18T06:26:38.294Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:26:38.294Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:26:38.296Z LOG: Shader compiled successfully.
2026-04-18T06:27:29.312Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:27:29.313Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:27:29.314Z LOG: Shader compiled successfully.
2026-04-18T06:32:02.685Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:32:02.686Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:32:02.687Z LOG: Shader compiled successfully.
2026-04-18T06:40:36.676Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:40:36.676Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:40:36.678Z LOG: Shader compiled successfully.
2026-04-18T06:43:14.510Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:43:14.510Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:43:14.512Z LOG: Shader compiled successfully.
2026-04-18T06:44:48.679Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:44:48.680Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:44:48.682Z LOG: Shader compiled successfully.
2026-04-18T06:46:21.366Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:46:21.366Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:46:21.368Z LOG: Shader compiled successfully.
2026-04-18T06:58:43.883Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T06:58:43.883Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T06:58:43.884Z LOG: Shader compiled successfully.
2026-04-18T07:00:29.400Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:00:29.400Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:00:29.403Z LOG: Shader compiled successfully.
2026-04-18T07:05:17.982Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:05:17.983Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:05:17.985Z LOG: Shader compiled successfully.
2026-04-18T07:05:32.831Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:05:32.832Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:05:32.834Z LOG: Shader compiled successfully.
2026-04-18T07:08:06.045Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:08:06.045Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:08:06.048Z LOG: Shader compiled successfully.
2026-04-18T07:08:59.955Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:08:59.955Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:08:59.957Z LOG: Shader compiled successfully.
2026-04-18T07:12:16.625Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:12:16.626Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:12:16.627Z LOG: Shader compiled successfully.
2026-04-18T07:18:41.802Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:18:41.802Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:18:41.804Z LOG: Shader compiled successfully.
2026-04-18T07:30:02.372Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:30:02.372Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:30:02.374Z LOG: Shader compiled successfully.
2026-04-18T07:38:38.362Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:38:38.362Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:38:38.364Z LOG: Shader compiled successfully.
2026-04-18T07:50:25.779Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:50:25.779Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:50:25.781Z LOG: Shader compiled successfully.
2026-04-18T07:55:14.187Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:55:14.188Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:55:14.189Z LOG: Shader compiled successfully.
2026-04-18T07:55:55.432Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T07:55:55.433Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T07:55:55.435Z LOG: Shader compiled successfully.
2026-04-18T08:02:10.154Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T08:02:10.155Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T08:02:10.156Z LOG: Shader compiled successfully.
2026-04-18T08:02:22.492Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T08:02:22.493Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T08:02:22.494Z LOG: Shader compiled successfully.
2026-04-18T08:02:57.482Z WARN: GPU device lost: A valid external Instance reference no longer exists. (reason: unknown)
2026-04-18T08:02:57.482Z WARN: GPU device lost: A valid external Instance reference no longer exists. (reason: unknown)
2026-04-18T08:03:02.500Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T08:03:02.501Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T08:03:02.502Z LOG: Shader compiled successfully.
2026-04-18T08:05:11.937Z WARN: GPU device lost: A valid external Instance reference no longer exists. (reason: unknown)
2026-04-18T08:05:44.078Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T08:05:44.079Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T08:05:44.081Z LOG: Shader compiled successfully.
2026-04-18T08:06:55.355Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T08:06:55.355Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T08:06:55.357Z LOG: Shader compiled successfully.
2026-04-18T10:40:08.022Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T10:40:08.022Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T10:40:08.024Z LOG: Shader compiled successfully.
2026-04-18T10:42:51.395Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T10:42:51.395Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T10:42:51.397Z LOG: Shader compiled successfully.
2026-04-18T10:44:21.380Z LOG: Loaded shader.wgsl (1227 chars)
2026-04-18T10:44:21.380Z LOG: @group(0) @binding(0) var<uniform> viewProj : mat4x4<f32>;

struct VertexInput {
  @location(0) position : vec3<f32>,
  @location(1) normal   : vec3<f32>,
  @location(2) model0   : vec4<f32>,
  @location(3) model1   : vec4<f32>,
  @location(4) model2   : vec4<f32>,
  @location(5) model3   : vec4<f32>,
  @location(6) color    : vec4<f32>,
};

struct VertexOutput {
  @builtin(position) position : vec4<f32>,
  @location(0) color          : vec4<f32>,
  @location(1) worldNormal    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var output : VertexOutput;
  output.position = viewProj * worldPos;
  output.color = input.color;
  output.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  return output;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let diffuse  = max(dot(normalize(input.worldNormal), lightDir), 0.0);
  let light    = ambient + (1.0 - ambient) * diffuse;
  return vec4<f32>(input.color.rgb * light, input.color.a);
}

2026-04-18T10:44:21.382Z LOG: Shader compiled successfully.
2026-04-18T10:52:54.036Z LOG: Loaded shader.wgsl (3159 chars)
2026-04-18T10:52:54.036Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: deterministic from axis-aligned face normal
  var T : vec3<f32>;
  if (absN.x > 0.5) { T = vec3<f32>(0.0, 1.0, 0.0); }
  else               { T = vec3<f32>(1.0, 0.0, 0.0); }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas   = step(0.0, input.tileIndex);
  let finalColor = mix(input.color, texColor, useAtlas);
  let finalNorm  = mix(N, bumpNormal, useAtlas);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T10:52:54.038Z LOG: Shader compiled successfully.
2026-04-18T10:55:04.376Z LOG: Loaded shader.wgsl (3165 chars)
2026-04-18T10:55:04.376Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: deterministic from axis-aligned face normal
  var T : vec3<f32>;
  if (absN.x > 0.5) { T = vec3<f32>(0.0, 1.0, 0.0); }
  else               { T = vec3<f32>(1.0, 0.0, 0.0); }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas   = step(0.0, input.tileIndex);
  let finalColor = mix(input.color, texColor, useAtlas);
  let finalNorm  = mix(N, bumpNormal, useAtlas);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T10:55:04.378Z LOG: Shader compiled successfully.
2026-04-18T11:02:36.704Z LOG: Loaded shader.wgsl (3165 chars)
2026-04-18T11:02:36.704Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: deterministic from axis-aligned face normal
  var T : vec3<f32>;
  if (absN.x > 0.5) { T = vec3<f32>(0.0, 1.0, 0.0); }
  else               { T = vec3<f32>(1.0, 0.0, 0.0); }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas   = step(0.0, input.tileIndex);
  let finalColor = mix(input.color, texColor, useAtlas);
  let finalNorm  = mix(N, bumpNormal, useAtlas);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:02:36.706Z LOG: Shader compiled successfully.
2026-04-18T11:03:04.811Z LOG: Loaded shader.wgsl (3165 chars)
2026-04-18T11:03:04.811Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: deterministic from axis-aligned face normal
  var T : vec3<f32>;
  if (absN.x > 0.5) { T = vec3<f32>(0.0, 1.0, 0.0); }
  else               { T = vec3<f32>(1.0, 0.0, 0.0); }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas   = step(0.0, input.tileIndex);
  let finalColor = mix(input.color, texColor, useAtlas);
  let finalNorm  = mix(N, bumpNormal, useAtlas);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:03:04.813Z LOG: Shader compiled successfully.
2026-04-18T11:03:36.723Z LOG: Loaded shader.wgsl (3165 chars)
2026-04-18T11:03:36.723Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: deterministic from axis-aligned face normal
  var T : vec3<f32>;
  if (absN.x > 0.5) { T = vec3<f32>(0.0, 1.0, 0.0); }
  else               { T = vec3<f32>(1.0, 0.0, 0.0); }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas   = step(0.0, input.tileIndex);
  let finalColor = mix(input.color, texColor, useAtlas);
  let finalNorm  = mix(N, bumpNormal, useAtlas);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:03:36.725Z LOG: Shader compiled successfully.
2026-04-18T11:07:59.014Z LOG: Loaded shader.wgsl (3336 chars)
2026-04-18T11:07:59.014Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas   = step(0.0, input.tileIndex);
  let finalColor = mix(input.color, texColor, useAtlas);
  let finalNorm  = mix(N, bumpNormal, useAtlas);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:07:59.016Z LOG: Shader compiled successfully.
2026-04-18T11:09:24.376Z LOG: Loaded shader.wgsl (3485 chars)
2026-04-18T11:09:24.376Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:09:24.377Z LOG: Shader compiled successfully.
2026-04-18T11:31:32.222Z LOG: Loaded shader.wgsl (3485 chars)
2026-04-18T11:31:32.223Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:31:32.224Z LOG: Shader compiled successfully.
2026-04-18T11:34:56.327Z LOG: Loaded shader.wgsl (3485 chars)
2026-04-18T11:34:56.327Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:34:56.329Z LOG: Shader compiled successfully.
2026-04-18T11:41:54.037Z LOG: Loaded shader.wgsl (3485 chars)
2026-04-18T11:41:54.037Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:41:54.039Z LOG: Shader compiled successfully.
2026-04-18T11:54:39.269Z LOG: Loaded shader.wgsl (3485 chars)
2026-04-18T11:54:39.269Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:54:39.271Z LOG: Shader compiled successfully.
2026-04-18T11:55:38.875Z LOG: Loaded shader.wgsl (3485 chars)
2026-04-18T11:55:38.877Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:55:38.877Z LOG: Shader compiled successfully.
2026-04-18T11:56:43.547Z LOG: Loaded shader.wgsl (3485 chars)
2026-04-18T11:56:43.547Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:56:43.549Z LOG: Shader compiled successfully.
2026-04-18T11:57:51.087Z LOG: Loaded shader.wgsl (3485 chars)
2026-04-18T11:57:51.087Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 8.0, uv.y / 8.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T11:57:51.089Z LOG: Shader compiled successfully.
2026-04-18T12:13:57.722Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:13:57.723Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:13:57.725Z LOG: Shader compiled successfully.
2026-04-18T12:20:59.715Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:20:59.717Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:20:59.717Z LOG: Shader compiled successfully.
2026-04-18T12:24:27.470Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:24:27.472Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:24:27.472Z LOG: Shader compiled successfully.
2026-04-18T12:25:20.965Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:25:20.965Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:25:20.967Z LOG: Shader compiled successfully.
2026-04-18T12:25:21.278Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:25:21.278Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:25:21.279Z LOG: Shader compiled successfully.
2026-04-18T12:27:22.724Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:27:22.724Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:27:22.725Z LOG: Shader compiled successfully.
2026-04-18T12:27:22.966Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:27:22.967Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:27:22.968Z LOG: Shader compiled successfully.
2026-04-18T12:27:23.218Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:27:23.219Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:27:23.220Z LOG: Shader compiled successfully.
2026-04-18T12:27:23.610Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:27:23.610Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:27:23.610Z LOG: Shader compiled successfully.
2026-04-18T12:27:24.835Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:27:24.836Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:27:24.837Z LOG: Shader compiled successfully.
2026-04-18T12:30:20.602Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T12:30:20.603Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T12:30:20.605Z LOG: Shader compiled successfully.
2026-04-18T12:31:01.858Z ERROR: Unhandled promise rejection: SecurityError: Pointer lock cannot be acquired immediately after the user has exited the lock.
2026-04-18T12:31:01.867Z ERROR: Unhandled promise rejection: SecurityError: Pointer lock cannot be acquired immediately after the user has exited the lock.
2026-04-18T13:15:56.200Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T13:15:56.201Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T13:15:56.203Z LOG: Shader compiled successfully.
2026-04-18T13:18:39.895Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T13:18:39.895Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T13:18:39.897Z LOG: Shader compiled successfully.
2026-04-18T13:18:48.624Z ERROR: Unhandled promise rejection: SecurityError: Pointer lock cannot be acquired immediately after the user has exited the lock.
2026-04-18T13:18:48.625Z ERROR: Unhandled promise rejection: SecurityError: Pointer lock cannot be acquired immediately after the user has exited the lock.
2026-04-18T13:20:37.111Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T13:20:37.111Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T13:20:37.113Z LOG: Shader compiled successfully.
2026-04-18T13:20:44.724Z ERROR: Unhandled promise rejection: SecurityError: Pointer lock cannot be acquired immediately after the user has exited the lock.
2026-04-18T13:20:44.726Z ERROR: Unhandled promise rejection: SecurityError: Pointer lock cannot be acquired immediately after the user has exited the lock.
2026-04-18T15:16:58.628Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T15:16:58.630Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T15:16:58.631Z LOG: Shader compiled successfully.
2026-04-18T15:17:34.848Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T15:17:34.851Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T15:17:34.851Z LOG: Shader compiled successfully.
2026-04-18T15:24:26.695Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T15:24:26.695Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T15:24:26.696Z LOG: Shader compiled successfully.
2026-04-18T15:28:23.507Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T15:28:23.508Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T15:28:23.510Z LOG: Shader compiled successfully.
2026-04-18T15:32:44.587Z ERROR: Unhandled promise rejection: SecurityError: Pointer lock cannot be acquired immediately after the user has exited the lock.
2026-04-18T15:32:44.597Z ERROR: Unhandled promise rejection: SecurityError: Pointer lock cannot be acquired immediately after the user has exited the lock.
2026-04-18T15:48:41.226Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T15:48:41.227Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T15:48:41.229Z LOG: Shader compiled successfully.
2026-04-18T15:51:09.201Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-18T15:51:09.202Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-18T15:51:09.204Z LOG: Shader compiled successfully.
2026-04-26T09:19:10.646Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:19:10.650Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:19:10.651Z LOG: Shader compiled successfully.
2026-04-26T09:24:30.844Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:30.844Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:30.846Z LOG: Shader compiled successfully.
2026-04-26T09:24:31.809Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:31.810Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:31.811Z LOG: Shader compiled successfully.
2026-04-26T09:24:32.513Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:32.514Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:32.516Z LOG: Shader compiled successfully.
2026-04-26T09:24:32.801Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:32.801Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:32.803Z LOG: Shader compiled successfully.
2026-04-26T09:24:33.066Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:33.066Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:33.068Z LOG: Shader compiled successfully.
2026-04-26T09:24:33.289Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:33.290Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:33.291Z LOG: Shader compiled successfully.
2026-04-26T09:24:33.551Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:33.552Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:33.553Z LOG: Shader compiled successfully.
2026-04-26T09:24:33.795Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:33.796Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:33.797Z LOG: Shader compiled successfully.
2026-04-26T09:24:34.046Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:34.046Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:34.048Z LOG: Shader compiled successfully.
2026-04-26T09:24:34.278Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:34.279Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:34.280Z LOG: Shader compiled successfully.
2026-04-26T09:24:35.239Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T09:24:35.240Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T09:24:35.241Z LOG: Shader compiled successfully.
2026-04-26T12:04:11.654Z LOG: Loaded shader.wgsl (3487 chars)
2026-04-26T12:04:11.655Z LOG: @group(0) @binding(0) var<uniform> viewProj  : mat4x4<f32>;
@group(0) @binding(1) var atlasSampler       : sampler;
@group(0) @binding(2) var atlasTex           : texture_2d<f32>;
@group(0) @binding(3) var normalTex          : texture_2d<f32>;

struct VertexInput {
  @location(0) position  : vec3<f32>,
  @location(1) normal    : vec3<f32>,
  @location(2) model0    : vec4<f32>,
  @location(3) model1    : vec4<f32>,
  @location(4) model2    : vec4<f32>,
  @location(5) model3    : vec4<f32>,
  @location(6) color     : vec4<f32>,
  @location(7) tileIndex : f32,
};

struct VertexOutput {
  @builtin(position)              clipPos     : vec4<f32>,
  @location(0)                    color       : vec4<f32>,
  @location(1)                    worldNormal : vec3<f32>,
  @location(2) @interpolate(flat) tileIndex   : f32,
  @location(3)                    localPos    : vec3<f32>,
};

@vertex fn main(input : VertexInput) -> VertexOutput {
  let model    = mat4x4<f32>(input.model0, input.model1, input.model2, input.model3);
  let worldPos = model * vec4<f32>(input.position, 1.0);
  var out : VertexOutput;
  out.clipPos     = viewProj * worldPos;
  out.color       = input.color;
  out.worldNormal = normalize((model * vec4<f32>(input.normal, 0.0)).xyz);
  out.tileIndex   = input.tileIndex;
  out.localPos    = input.position;
  return out;
}

@fragment fn frag(input : VertexOutput) -> @location(0) vec4<f32> {
  let lightDir = normalize(vec3<f32>(0.55, 1.0, 0.35));
  let ambient  = 0.25;
  let N        = normalize(input.worldNormal);
  let absN     = abs(N);

  // --- UV from face orientation ---
  var uv : vec2<f32>;
  if (absN.y > 0.5) {
    uv = fract(input.localPos.xz + 0.5);
  } else if (absN.x > 0.5) {
    uv = fract(input.localPos.zy + 0.5);
  } else {
    uv = fract(input.localPos.xy + 0.5);
  }

  // --- Select atlas column (handle grass/snow face variants) ---
  var col = input.tileIndex;
  if (absN.y > 0.5 && N.y < 0.0) {
    // bottom face of grass or snow → dirt
    if (col == 0.0 || col == 4.0) { col = 2.0; }
  } else if (absN.y < 0.5) {
    // side face of grass or snow → next tile column
    if (col == 0.0 || col == 4.0) { col = col + 1.0; }
  }

  let atlasUV = vec2<f32>((col + uv.x) / 10.0, uv.y / 10.0);

  // --- Sample colour atlas ---
  let texColor = textureSample(atlasTex, atlasSampler, atlasUV);

  // --- Sample & apply normal map ---
  let nSample = textureSample(normalTex, atlasSampler, atlasUV).xyz * 2.0 - 1.0;

  // TBN: tangent aligned with face's horizontal UV axis so normal-map
  // horizontal variation maps to horizontal world perturbation on all faces.
  var T : vec3<f32>;
  if (absN.x > 0.5) {
    // X-face UV: uv.x = localPos.z → tangent along ±Z
    T = vec3<f32>(0.0, 0.0, select(-1.0, 1.0, N.x < 0.0));
  } else {
    T = vec3<f32>(1.0, 0.0, 0.0);
  }
  let B          = cross(N, T);
  let bumpNormal = normalize(T * nSample.x + B * nSample.y + N * nSample.z);

  // --- Blend: atlas for blocks (tileIndex >= 0), vertex color for creatures/dynamic ---
  let useAtlas    = step(0.0, input.tileIndex);
  let finalColor  = mix(input.color, texColor, useAtlas);
  // Only apply bump normal on top/bottom faces — sides look banded with mostly-overhead light
  let applyBump   = useAtlas * step(0.5, absN.y);
  let finalNorm   = mix(N, bumpNormal, applyBump);

  let diffuse = max(dot(finalNorm, lightDir), 0.0);
  let light   = ambient + (1.0 - ambient) * diffuse;

  return vec4<f32>(finalColor.rgb * light, finalColor.a);
}

2026-04-26T12:04:11.657Z LOG: Shader compiled successfully.
